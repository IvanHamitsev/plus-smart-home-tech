@namespace("ru.yandex.practicum.kafka.telemetry.event")
protocol ClimateSensorEventProtocol {
	record ClimateSensorEvent {
		string id;
		string hub_id;
		timestamp_ms timestamp;
		int temperatureC;
		int humidity;
		int co2Level;
		string type;
	}
}