@namespace("ru.yandex.practicum.kafka.telemetry.event")
protocol MotionSensorEventProtocol {
	record MotionSensorEvent {
		string id;
		string hub_id;
		timestamp_ms timestamp;
		int linkQuality;
		boolean motion;
		int voltage;
		string type;
	}
}